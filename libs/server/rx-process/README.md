# rx-process

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rx-process` to build the library.

## Running unit tests

Run `nx test rx-process` to execute the unit tests via [Jest](https://jestjs.io).
