import { observableChildProcess } from './observable-child-process'

describe('observableChildProcess', () => {
  it('should work', () => {
    expect(observableChildProcess()).toEqual('observable-child-process')
  })
})
