# fastify-plugin-stream

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fastify-plugin-stream` to build the library.

## Running unit tests

Run `nx test fastify-plugin-stream` to execute the unit tests via [Jest](https://jestjs.io).
