# fastify-plugin-library

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fastify-plugin-library` to build the library.

## Running unit tests

Run `nx test fastify-plugin-library` to execute the unit tests via [Jest](https://jestjs.io).
