# fastify-plugin-playlist

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fastify-plugin-playlist` to build the library.

## Running unit tests

Run `nx test fastify-plugin-playlist` to execute the unit tests via [Jest](https://jestjs.io).
