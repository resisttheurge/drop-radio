# playlist

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build playlist` to build the library.

## Running unit tests

Run `nx test playlist` to execute the unit tests via [Vitest](https://vitest.dev/).
